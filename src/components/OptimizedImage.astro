---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';

interface Props {
  src: ImageMetadata | string;
  alt: string;
  width?: number;
  height?: number;
  class?: string;
  loading?: 'lazy' | 'eager';
  quality?: number;
}

const {
  src,
  alt,
  width,
  height,
  class: className = '',
  loading = 'lazy',
  quality = 85,
} = Astro.props;

// Check if src is an imported image (ImageMetadata) or a public path (string)
const isImportedImage = typeof src !== 'string';
---

{isImportedImage ? (
  <Image
    src={src}
    alt={alt}
    width={width}
    height={height}
    quality={quality}
    loading={loading}
    format="webp"
    class={className}
    decoding="async"
  />
) : (
  <img
    src={src}
    alt={alt}
    width={width}
    height={height}
    loading={loading}
    decoding="async"
    class={className}
  />
)}
